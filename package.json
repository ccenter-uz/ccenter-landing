{
  "name": "ccenter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "scss-watch": "sass --watch scss:css",
    "serve-dev": "lite-server",
    "dev": "npm-run-all --parallel scss-watch serve-dev",
    "build": "sass scss:css --style compressed",
    "start": "serve . -p 8080"
  },
  "devDependencies": {
    "lite-server": "^2.6.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.62.0",
    "serve": "^14.2.0"
  }
}
